# UI campaign iteration

Owner FAIL (pre-UI-0): live home first screen failed apple.com product-page grammar. Continuous until appearance DoD green. Do not ask to stop.

Hard bans: no Seattle/Global; no founder; no /demo as product; do not rewrite thesis/offers/routes.

## Closed

- UI-0 **PASS**: Geist display tokens (weight 600, tracking `-0.03em` at 56–80px); owner palette; frosted 46px nav; near-black pill; no `#1d4ed8`. First-screen composition was still FAIL.

## This loop — UI-1

- Rebuild `/` first viewport only.
- Product STAGE is a fabricated Propose → Approve → Record console (Approve filled), not a rounded gray JOB/GATE text card alone.
- Services strip (Design / Build / Operations) sits in the first viewport.
- Soft ADD absorbed: Georgia / `ui-serif` fallbacks remapped where `--font-serif` already points at Geist/system.

## Next

- UI-2 dark gate rebuild after Leader PASS. Do not start in this slice.
