# Iteration after V3-13 PASS

Live preview: https://remex-atelier.vercel.app
V3-13 closed: content `1e1af30` / merge `4b080bd` / tip `e8c6cbd`. Unanimous PM + Leader PASS.

## Closed in V3-13

1. Root canvas locked (`bg-gutter`; no page-wash `bg-paper`).
2. 404 film shell; PulseLoop native film; Offers stub deleted.
3. Footer secondary Services/About wayfinding; Lab `aria-label="Lab prototypes"`.

## V3-14 ticket (this loop)

Synthesized from Apple / UX / Vision post-V3-13 KEEP/KILL/ADD (non-blocking ADD only).

1. **Home density** — H2 hire-still reads as one seat lighting up; H7 method columns get concrete still glyphs (kill empty `.home-method-col__glyph`); home Ledger still stays on score DNA (corpus / Propose-not-Execute / citations). Locked headlines verbatim.
2. **Contact** — form fields / checklist align to brief asks: Which workflow hurts. Who approves. What the agent may never do. What record you must keep. (from `docs/scroll-score-v3.md` H6). Keep Name/Email; expand Brief into those four asks (or labeled checklist + brief). Hit targets ≥44.
3. **Services** — three offers (Agent Apps / Agent Ops / Advisory) each nail a non-interchangeable gate or record fact; no interchangeable filler.
4. **About** — echo SAY line verbatim: `One painful workflow. One agent. A human gate.` Keep legal/geography; no founder; no 机羽云 move.
5. **Soft (same slice if cheap)** — drop dead `.lab-loop` remap if unused; optional `.btn-primary` text color `#fff` instead of `var(--paper)`.

## Hard bans (unchanged)

- No English-products market claims; no founder name; no CumuLabs/outsourcing; no Lenis/normalizeScroll/body lock; H3-only pin with pinOk; locked copy verbatim.
- Do not move 机羽云 or the refuse list.
- No `/demo` marketing CTAs; no long pins; no new isomorphic six-clone film.
