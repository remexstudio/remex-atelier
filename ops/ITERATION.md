# UI campaign iteration

Owner FAIL: live home first screen still fails apple.com product-page grammar (soft poster type and/or brochure first screen). Continuous until appearance DoD green. Do not ask to stop.

Hard bans: no Seattle/Global; no founder; no /demo as product; do not rewrite thesis/offers/routes.

## This loop

- UI-0 delivered: Geist-only layout font; Newsreader/Source Sans display killed; display headlines locked to weight 600; large-size tracking token `-0.03em`; owner palette (`--bg` `#ffffff`, `--gutter` `#f5f5f7`, `--ink` `#1d1d1f`, `--secondary` `#6e6e73`, `--line` `rgba(0,0,0,0.08)`, `--dark` `#000`, `--dark-ink` `#f5f5f7`, `--cta` `#1d1d1f`); nav 46px `rgba(255,255,255,0.72)` + `blur(20px) saturate(180%)`; pill CTA token; residual serif slot remapped to Geist/system.
- First screen composition still FAIL until UI-1. Tokens only this slice.
- Next: UI-1 stage / first viewport after Leader PASS. Do not start UI-1 in this slice.
