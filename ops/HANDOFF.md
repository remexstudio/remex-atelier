# Handoff

- TASK ID: V7-0
- Status: DEV implementing — awaiting local `pnpm build` + home smoke
- Change: Appearance only. Geist/system display tokens. Home first viewport is promise + black-pill CTA + product STAGE (Propose → Approve → Record, Approve filled) + Design / Build / Operations strip. JOB/GATE gray card removed from hero. Method restyled on `#f5f5f7` with no card. Offers, routes, and thesis sentences unchanged.
- Parked: V6-5 / V6-6 remain parked
- Do not start V7-1 until Leader PASS
