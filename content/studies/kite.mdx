# Kite & Co.

Editorial study copy lives in `app/work/kite/page.tsx` for this slice
(TSX study page — MDX wired later if needed). Product: Morning Remex. Demo
paths: Brief (Priority / Watch / Noise), Thread (two tones + escalate), Digest
(record only — never auto-publish).
